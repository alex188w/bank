apiVersion: v1
kind: Service
metadata:
  name: {{ include "exchange-service.fullname" . }}
  labels:
    app: {{ include "exchange-service.name" . }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
      nodePort: 30084   # <--- добавь этот параметр
  selector:
    app: {{ include "exchange-service.name" . }}