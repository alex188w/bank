apiVersion: v1
kind: Service
metadata:
  name: {{ include "cash-service.fullname" . }}
  labels:
    app: {{ include "cash-service.name" . }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
      nodePort: 30083   # <--- добавь этот параметр
  selector:
    app: {{ include "cash-service.name" . }}