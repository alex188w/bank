apiVersion: v1
kind: Service
metadata:
  name: {{ include "zipkin.fullname" . }}
  labels:
    app: {{ include "zipkin.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "zipkin.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 9411