apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "exchange-generator.fullname" . }}-config
data:
  application-k8s.yml: |
    spring:
      application:
        name: exchange-generator

    server:
      port: 8085

    management:
      health:
        db:
          enabled: false
        r2dbc:
          enabled: false