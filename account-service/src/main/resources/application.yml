spring:
  application:
    name: account-service

  r2dbc:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}

  main:
    web-application-type: reactive

server:
  port: ${ACCOUNT_PORT:8081}

logging:
  level:
    root: INFO